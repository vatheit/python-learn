
"""
id(__name__)

hash(__name__)

type(__name__)

help(__name__)

float int complex
bin/hex/oct
bool
list/tuple/dict/set
bytes/bytearray

input([prompt])

print(*objects, sep='', end='\n', file=sys.stdout,flush=False)

len
isinstance(obj,class_or_tuple)
issubclass


abs
max/min
round
pow
divmod    tuple(x//y, x%y)
sum
"""

round(-0.5)
round(0.5)
round(-1.5)
round(1.5)

"""
range(start,stop[,step])
"""

"""
整数转字符   chr(i)
字符转整数   ord(a)

str()
repr
ascii


sorted(iterable[, key][,reverse])

返回一个翻转原色的迭代器    reversed(seq)
枚举  enumerate(seq, start=0)

迭代器 iter(iterable)
从迭代器中取元素 next(iterator[, default])

可迭代对象： 
list tuple string bytes bytearray range set dict 生成器
可使用成员操作符in  not in

拉链函数zip(*iterables)
将多个可迭代对象合并在一起
"""




